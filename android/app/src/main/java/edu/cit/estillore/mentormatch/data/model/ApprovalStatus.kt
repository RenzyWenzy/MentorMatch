package edu.cit.estillore.mentormatch.data.model

/** Mirrors the tutor-profile approval workflow (TutorProfileController.java). */
enum class ApprovalStatus {
    PENDING,
    APPROVED,
    REJECTED
}
